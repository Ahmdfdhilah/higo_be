# Railway Environment Variables Template
# Copy these to Railway dashboard > Variables

# Server Configuration
NODE_ENV=production
PORT=$PORT

# Database - Use Railway MongoDB plugin or external MongoDB Atlas
MONGODB_URI=$MONGODB_URL

# Redis - Use Railway Redis plugin or external Redis
REDIS_URL=$REDIS_URL

# JWT Configuration
JWT_SECRET=$JWT_SECRET
JWT_EXPIRES_IN=7d

# CORS Configuration
FRONTEND_URL=https://your-frontend-domain.com

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# File Upload Configuration (use absolute path on Railway)
UPLOAD_DIR=/app/uploads
MAX_FILE_SIZE=200